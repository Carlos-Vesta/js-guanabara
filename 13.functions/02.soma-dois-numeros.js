
function soma(numero1, numero2) { // Os parâmetros podem ser atribuídos valores. Ex: numero1 = 0, numero2 = 0

    return `A soma entre ${numero1} e ${numero2} é igual à ${(numero1 + numero2)}`;
};

console.log(soma(3, 6)); // Caso receba apenas um argumento, irá retornar NaN