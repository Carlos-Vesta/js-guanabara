// Faça um programa que leia um número e mostre a sua Raiz Quadrado.

var numero = Number(prompt('Digite um número: '));

var raizQuadrada = Math.sqrt(numero);

alert(`A raiz quadrada de ${numero} é igual a ${raizQuadrada}`);