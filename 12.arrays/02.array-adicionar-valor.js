
let num = [5, 7, 4, 12, 6, 8];

// Irá colocar o 11 na posição 2, removendo o que já lá está
num[2] = 11;

// Irá colocar o 20 no final da lista
num.push(20);

// Irá mostrar o resultado dentro dos parenteses rectos (formatação padrão)
// console.log(num);

// Irá mostrar o resultado sem a formatação padrão
console.log(`${num}`);

